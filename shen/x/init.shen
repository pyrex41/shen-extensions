\\ shen.x package init — load every extension in this package.
\\ Prefer (load "load.shen") from the repo root; this file is the same
\\ entry when *home-directory* is already the extensions root.

(load "shen/x/sha256.shen")
(load "shen/x/zmq.shen")
